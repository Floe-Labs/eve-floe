TODO: record docs/demo.gif (the README embeds it).

This needs a live Eve env + two funded Floe keys, so it can't be generated here.

What to capture (≈15s screen recording of `npx eve dev`):
1. Ask the agent to research a topic (e.g. "compare 3 vector databases").
2. The lead delegates to the `fetcher` subagent.
3. The fetcher pays a few x402 API calls (the running spend shows).
4. As it nears its cap, it tapers (fetches fewer / only the top source) — the
   budget advisory line is visible.
5. It stops at the cap; the final spend is under the budget. The "runaway" never
   happens.

Save the recording as docs/demo.gif (keep the filename so the README embed
resolves). Optimize to a reasonable size (< ~3 MB).
