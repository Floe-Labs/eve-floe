# Floe keys are the ONLY secrets an Eve × Floe agent needs. Create them (each with
# its own budget cap) at https://dev-dashboard.floelabs.xyz — the agents hold no
# provider/vendor keys.
FLOE_AGENT_KEY=floe_xxxxxxxxxxxxxxxx     # lead agent's budget
FLOE_FETCHER_KEY=floe_yyyyyyyyyyyyyyyy   # fetcher subagent's own (smaller) budget
